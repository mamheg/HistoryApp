# Telegram Bot для History Coffee

Этот бот работает параллельно с мини-приложением History Coffee и предоставляет:
- Приветственные сообщения для новых пользователей
- Уведомления о подтверждении заказов
- Inline-кнопки для оплаты заказов
- Уведомления о готовности заказа
- Просмотр истории заказов и профиля

## 📁 Структура

```
bot/
├── __init__.py
├── config.py          # Конфигурация бота
├── database.py        # Работа с базой данных
├── keyboards.py       # Клавиатуры и inline-кнопки
├── main.py            # Главный файл бота
├── requirements.txt   # Зависимости Python
├── .env.example       # Пример конфигурации
└── handlers/          # Обработчики команд
    ├── __init__.py
    ├── start.py       # Команды /start, /help, /profile, /orders
    ├── orders.py      # Уведомления о заказах
    └── admin.py       # Админ-команды
```

## 🚀 Установка и настройка

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 2. Настройка Mini App

1. В @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Перейдите в `Bot Settings` → `Menu Button` → `Configure Menu Button`
4. Установите:
   - **Button Text**: `☕ Меню`
   - **Web App URL**: `https://your-frontend-url.com` (URL вашего фронтенда)

### 3. Настройка окружения

```bash
cd bot

# Создайте файл .env на основе .env.example
cp .env.example .env

# Отредактируйте .env и добавьте ваш токен
# BOT_TOKEN=ваш_токен_здесь
```

### 4. Установка зависимостей

```bash
# Создайте виртуальное окружение (опционально, но рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 5. Запуск бота

```bash
python main.py
```

Бот автоматически начнёт:
- Отвечать на команды пользователей
- Мониторить базу данных на предмет новых заказов
- Отправлять уведомления о подтверждении заказа с кнопкой оплаты
- Отправлять уведомления о готовности заказа (по команде админа)

## 📋 Команды бота

### Для пользователей:

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню с приветствием History Coffee |
| `/menu` | Открыть мини-приложение для заказа |
| `/help` | Справка по боту |
| `/profile` | Профиль пользователя и бонусы |
| `/orders` | История заказов |

### Для администраторов:

| Команда | Описание |
|---------|----------|
| `/admin` | Посмотреть последние заказы |
| `/notify ORDER_ID` | Отправить уведомление о готовности заказа |

**Admin IDs**: `1962824399`, `937710441` (изменяется в `config.py`)

## 🔔 Как работает уведомление о заказах

1. Пользователь оформляет заказ в мини-приложении
2. Заказ сохраняется в базе данных SQLite
3. Бот автоматически обнаруживает новый заказ (каждые 5 секунд)
4. Бот отправляет пользователю сообщение с:
   - Деталями заказа
   - Inline-кнопкой "Оплатить"
   - Ссылкой на мини-приложение

## 💳 Оплата

По умолчанию используется заглушка для ссылки оплаты:
```
https://historycoffee.payment/order/{order_id}
```

Для реальной оплаты:
1. Интегрируйте платёжную систему (ЮKassa, CloudPayments, etc.)
2. Измените `PAYMENT_LINK_TEMPLATE` в `bot/config.py`
3. Добавьте обработку вебхуков от платёжной системы

## 🗄️ Работа с базой данных

Бот использует ту же базу данных, что и бэкенд мини-приложения:
- **Путь**: `backend/hoffee.db`
- **Тип**: SQLite

Бот **только читает** из базы данных и **не изменяет** существующую структуру.

## ⚙️ Конфигурация

Основные настройки в `bot/config.py`:

```python
BOT_TOKEN = "your_token_here"  # Токен бота
ADMIN_IDS = [1962824399, 937710441]  # ID администраторов
PAYMENT_LINK_TEMPLATE = "https://..."  # Шаблон ссылки оплаты
MINI_APP_URL = "https://t.me/YourBot/app"  # Ссылка на мини-приложение
```

## 🐛 Решение проблем

### Бот не отвечает на команды

1. Проверьте, что токен указан правильно в `.env`
2. Убедитесь, что бот запущен (`python main.py`)
3. Проверьте логи на наличие ошибок

### Не приходят уведомления о заказах

1. Проверьте, что путь к базе данных правильный в `config.py`
2. Убедитесь, что база данных существует и доступна
3. Посмотрите логи бота - там должно быть сообщение "New order detected"

### Бот не может отправить сообщение пользователю

1. Пользователь должен сначала запустить бота командой `/start`
2. Проверьте, что пользователь не заблокировал бота

## 📝 Дополнительная информация

- **Фреймворк**: aiogram 3.x
- **Python**: 3.9+
- **База данных**: SQLite (совместима с существующей)
- **Метод получения обновлений**: Polling (для WebSocket/Webhook нужно доработать)

## 🔗 Полезные ссылки

- [aiogram Documentation](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)
